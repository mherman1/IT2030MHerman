@model IEnumerable<MVCMusicStoreApplication.Models.Genre>

@{
    ViewBag.Title = "Browse";
}

<h2>Browse</h2>

<div class="panel-body">

    <h2>Browse By Genre</h2>

    <ul>
        @foreach (var item in Model)
        {
            <li>
                @Html.ActionLink(item.Name, "Index", new { id = item.GenreId })
            </li>
        }
    </ul>

</div>